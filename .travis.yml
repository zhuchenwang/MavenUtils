language: java

jdk:
    - oraclejdk7

install: ./gradlew clean classes

script: ./gradlew test jacocoTestReport

after_script: ./gradlew publish

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/