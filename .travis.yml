language: java

jdk:
    - oraclejdk7

before_install:
    - chmod +x gradlew

script: ./gradlew cleant test jacocoTestReport publish

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/